/// Placeholder data source for the diagnosis feature.
///
/// All diagnosis logic is pure rule-based evaluation with no external data
/// dependencies. This datasource exists for architectural consistency and
/// future extensibility (e.g. caching, user preferences).
class DiagnosisDataSource {
  const DiagnosisDataSource();
}
